The 3 css files are from bootstrap 4.3.1

documentation for everything can be found on the bootstrap documentation site
https://getbootstrap.com/docs/

It has all utilities except for embed
additionaly it also has the components: (button, cards, badges)

In the scss file there are 3 .bat files which will generate the custom selected css files.

sizings(padding and margins);
mostUtils(all utils except flex, sizings, maginsNpadding and embed);
grid(grid system and flex util);